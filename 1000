/* 
문제
두 정수 A와 B를 입력받은 다음, A+B를 출력하는 프로그램을 작성하시오.

입력
첫째 줄에 A와 B가 주어진다. (0 < A, B < 10)

출력
첫째 줄에 A+B를 출력한다.
*/

/* python 3 */
a, b = map(int, input().split())
print(a+b)

/* C */
#include <stdio.h>

int main() {
    int A, B;

    // 첫째 줄에 A와 B가 주어진다. (0 < A, B < 10)
    scanf("%d %d", &A, &B);

    // 둘째 줄에 A+B를 출력한다.
    printf("%d\n", A + B);

    return 0;
}
